package Game;

import Rooms.Room;

public class Board {
    private Room[][] map;

    public Board(Room[][] map) {
        this.map = map;
    }

    public class print() {
        for(int x = 0; x > map.length; x++){
            for(int y = 0; map[y])
        }
    }

}
